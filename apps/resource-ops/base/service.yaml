apiVersion: v1
kind: Service
metadata:
  name: resource-app
  labels:
    app.kubernetes.io/name: resource-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: resource-app
  ports:
    - name: http
      port: 80
      targetPort: http
